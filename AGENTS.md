# 项目协作规则

## 浏览器操作

- 优先使用 WebAgentMate 自身提供的浏览器工具。内置智能体使用 `browser_*`，Codex 使用 `wam_browser_*`，Claude 使用 `mcp__webmate__browser_*`。
- 除非用户明确指定，否则不要使用 Playwriter 的技能、CLI 或 MCP，也不要因其他技能推荐它而自动切换过去。
- 先检查当前会话实际提供的工具，再执行网页任务。工具名称出现在源码或文档中，不代表已接入当前会话。
- 自有工具未连接或缺少能力时，先检查本项目的工具接入、连接助手和运行状态；说明具体缺少的接口，不把安装或连接 Playwriter 当作前置条件。
- 先列出相关标签页并读取当前页面，使用返回的标签页、快照和元素标识操作，操作后再读取验证。不得凭空构造执行结果。
- 操作授权由用户选定的授权模式和工具执行器处理，已获授权的任务不重复索要许可。

## 面向用户的文案

- 说明已经提供的功能、实际费用、数据处理和推广佣金关系。
- 不主动为没有提供的奖励、额度或功能添加否定式声明；只有用户询问或具体情况需要解释时再说明。

## 规则生效范围

- 本文件约束在此仓库工作的开发智能体。
- 插件内智能体的浏览器工具规则集中在 `src/agent/browser-instructions.ts`，由内置智能体、Claude 和 Codex 运行时加载。修改时检查三个入口。
- 提示词不能接通缺失的工具，也不能替代执行层权限。不要宣称修改文档后外部 Codex 就已具备浏览器操作能力。
