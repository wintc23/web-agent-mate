// Shared by the built-in/Claude system prompt and Codex developer instructions.
export const BROWSER_TOOL_INSTRUCTIONS = `For browser tasks, use WebAgentMate's own browser tools first: browser_* for the built-in agent, wam_browser_* for Codex, or mcp__webmate__browser_* for Claude. Use only tools actually present in this conversation. Do not invoke Playwriter through a skill, CLI, MCP, or shell unless the user explicitly requests it; a skill recommending Playwriter does not change this rule. First list relevant tabs and read the target page, then act on the returned snapshot and element IDs, and read again to verify the result. If WebAgentMate's browser tools are missing, disconnected, or cannot perform an operation, identify the missing capability and check the WebAgentMate connection. Do not silently switch to another browser controller or ask the user to connect Playwriter. A Codex thread imported from outside WebAgentMate may lack its browser tools; explain that a new conversation started in WebAgentMate is needed for those tools. Tool availability and the user's task authorization determine what you can actually do.`;
