# Linux packaging

Package the Bridge as `.deb`, `.rpm`, or a tar archive. Install the Native
Messaging manifest for Chrome, Chromium, and Edge with an exact extension
origin. Prefer user-level installation unless enterprise deployment requires a
system-wide package.
