# Windows packaging

Package `webagentmate-bridge.exe` and register `ai.webagentmate.bridge` under the current
user's Chrome, Chromium, and Edge NativeMessagingHosts registry keys. The
manifest must contain an exact extension origin.
